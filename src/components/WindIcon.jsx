// 숨(Deep) 화면용 바람결 아이콘 — 호흡/바람 컨셉. 🧘(사람) 이모지를 대체.
// public/wind.png (흰 배경 제거·크롭한 투명 PNG)를 사용.
export default function WindIcon({ className }) {
  return <img className={className} src="/wind.png" alt="" aria-hidden="true" />;
}
